#include <iostream>
#include <iomanip>
using namespace std;

// Display safest region
int getChoice() {
  int choice;
  cin >> choice;
  if (choice != 1 || choice != 2 || choice != 3) {
    return -1;
  } else {
    return choice;
  }
}

void getAccidentStats(double& north, double& south, double$ east, double& west) {

double northNumber;
double southNumber;
double eastNumber;
double westNumber;

cout << "Enter the accident stats for the North region:" << endl;
cin >> northNumber;
if (northNumber !> 0 && northNumnber !< 1) {
  exit(EXIT_FAILURE)
} else {
  cout << "

cout << "Enter the accident stats for the South region:" << endl;
cin >> southNumber;
if (northNumber !> 0 && northNumnber !< 1) {
  exit(EXIT_FAILURE)
}

cout << "Enter the accident stats for the East region:" << endl;
cin >> eastNumber;
if (northNumber !> 0 && northNumnber !< 1) {
  exit(EXIT_FAILURE)
}

cout << "Enter the accident stats for the West region:" << endl;
cin >> westNumber;
if (northNumber !> 0 && northNumnber !< 1) {
  exit(EXIT_FAILURE)
}

  int main() {



    return 0;
  }
